apiVersion: v1
kind: Namespace
metadata:
  name: staging
  labels:
    istio-injection: enabled
  annotations:
    # cluster-addon tier - must exist, labeled, before the app's own
    # Application (wave "1") creates pods into it
    argocd.argoproj.io/sync-wave: "-1"
